let sliderFadeVelocity=400;let sliderFadeType='out';function sliderTransition(toInactiveItem,toPrevItem,toActiveItem,toNextItem){toInactiveItem.style.transition=sliderFadeVelocity+'ms ease-'+sliderFadeType+' opacity,'+'transform 0.5s cubic-bezier(0.51, 0.18, 0.84, 0.18)';toPrevItem.style.transition=sliderFadeVelocity+'ms ease-'+sliderFadeType+' opacity,'+'transform 0.5s cubic-bezier(0.51, 0.18, 0.84, 0.18)';toActiveItem.style.transition=sliderFadeVelocity+'ms ease-'+sliderFadeType+' opacity,'+'transform 0.5s cubic-bezier(0.51, 0.18, 0.84, 0.18)';toNextItem.style.transition=sliderFadeVelocity+'ms ease-'+sliderFadeType+' opacity,'+'transform 0.5s cubic-bezier(0.51, 0.18, 0.84, 0.18)';let inactiveContain=document.querySelector('#sliderInactiveContain');let prevContain=document.querySelector('#prevSliderContain');let activeContain=document.querySelector('#activeSliderContain');let nextContain=document.querySelector('#nextSliderContain');toInactiveItem.style.opacity='0';toPrevItem.style.opacity='0';toActiveItem.style.opacity='0';toNextItem.style.opacity='0';setTimeout(function changeSliderItems(){inactiveContain.append(toInactiveItem);prevContain.append(toPrevItem);activeContain.append(toActiveItem);nextContain.append(toNextItem)},sliderFadeVelocity/2);setTimeout(function changeOpacityOfSliderItems(){toInactiveItem.style.opacity='1';toPrevItem.style.opacity='1';toActiveItem.style.opacity='1';toNextItem.style.opacity='1'},sliderFadeVelocity)}function slider(direction){let sliderItems=document.querySelectorAll('.sliderItem');let minItem=0;let maxItem=sliderItems.length;let prevItem=document.querySelector('div#prevSliderContain > .sliderItem');let activeItem=document.querySelector('div#activeSliderContain > .sliderItem');let nextItem=document.querySelector('div#nextSliderContain > .sliderItem');let selectorsItems=document.querySelectorAll('.selectors');let prevNum=Number(prevItem.id.slice(-1));let activeNum=Number(activeItem.id.slice(-1));let nextNum=Number(nextItem.id.slice(-1));let toReplaceNum;let toReplaceItem;selectorsItems[activeNum].id='';if(direction=='next'){toReplaceNum=(nextNum+1)%maxItem;toReplaceItem=document.querySelector('#sliderItem'+toReplaceNum);sliderTransition(prevItem,activeItem,nextItem,toReplaceItem);selectorsItems[nextNum].id='activeSelectors'}else if(direction=='prev'){toReplaceNum=activeNum==minItem+1?(maxItem-1):(prevNum-1)%maxItem;toReplaceItem=document.querySelector('#sliderItem'+toReplaceNum);sliderTransition(nextItem,toReplaceItem,prevItem,activeItem);selectorsItems[prevNum].id='activeSelectors'}}function sliderSelector(toActiveNum){let sliderItems=document.querySelectorAll('.sliderItem');let selectorsItems=document.querySelectorAll('.selectors');for(let i=0;i<sliderItems.length;i++){sliderItems[i].style.transition=sliderFadeVelocity+'ms ease-'+sliderFadeType+' opacity,'+'transform 0.5s cubic-bezier(0.51, 0.18, 0.84, 0.18)'}let minItem=0;let maxItem=sliderItems.length;let prevItem=document.querySelector('div#prevSliderContain > .sliderItem');let activeItem=document.querySelector('div#activeSliderContain > .sliderItem');let nextItem=document.querySelector('div#nextSliderContain > .sliderItem');let activeNum=Number(activeItem.id.slice(-1));let inactiveContain=document.querySelector('#sliderInactiveContain');let prevContain=document.querySelector('#prevSliderContain');let activeContain=document.querySelector('#activeSliderContain');let nextContain=document.querySelector('#nextSliderContain');let toPrevNum=toActiveNum==minItem?(maxItem-1):(toActiveNum-1);let toNextNum=(toActiveNum+1)%maxItem;let toPrevItem=document.querySelector('#sliderItem'+toPrevNum);let toActiveItem=document.querySelector('#sliderItem'+toActiveNum);let toNextItem=document.querySelector('#sliderItem'+toNextNum);selectorsItems[activeNum].id='';selectorsItems[toActiveNum].id='activeSelectors';prevItem.style.opacity='0';activeItem.style.opacity='0';nextItem.style.opacity='0';toPrevItem.style.opacity='0';toActiveItem.style.opacity='0';toNextItem.style.opacity='0';setTimeout(function changeSliderItems(){inactiveContain.append(prevItem);inactiveContain.append(activeItem);inactiveContain.append(nextItem);prevContain.append(toPrevItem);activeContain.append(toActiveItem);nextContain.append(toNextItem)},sliderFadeVelocity/2);setTimeout(function changeOpacityOfSliderItems(){prevItem.style.opacity='1';activeItem.style.opacity='1';nextItem.style.opacity='1';toPrevItem.style.opacity='1';toActiveItem.style.opacity='1';toNextItem.style.opacity='1'},sliderFadeVelocity)}